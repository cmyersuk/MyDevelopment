# Tasks for Side-by-Side Layout

- [ ] Restructure `frontend/index.html` to wrap name box and file panel in a flex container
- [ ] Update `frontend/css/styles.css` to implement flexbox side-by-side layout for desktop
- [ ] Add responsive CSS media queries to stack panels on mobile (<768px)
- [ ] Write layout test in `frontend/tests/ui/layout.spec.js` to verify desktop flex alignment
- [ ] Manual test: view layout at desktop, tablet, and mobile widths in Safari
- [ ] Verify file panel scrolls independently without affecting name box
- [ ] Run all tests to ensure existing features still work
